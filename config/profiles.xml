<?xml version="1.0" ?>
<profiles>
	<profile id="tsfw">
		<!-- Process XLS configuration -->
		<action bean="F102" />
	</profile>

	<profile id="kindle">
		<!-- Process the book download configuration -->
		<action bean="F201" />

		<!-- Clone structure of kindle book -->
		<action bean="F202" />

		<!-- Process content and generate book data -->
		<action bean="F203" />

		<!-- Working with tool [kindlegen] -->
		<action bean="F204" />
	</profile>

	<!-- This tool will load a form for user check all the new information
	     IF the subject is comic, tool provide a download function book via kindle
	  -->
	<profile id="notification">
		<!-- Process notifier configuration -->
		<action bean="F301" />
	</profile>

	<!-- This tool will load a form for user check all the new information
	     IF the subject is comic, tool provide a download function book via kindle
	  -->
	<profile id="kpic">
		<!-- Process notifier configuration -->
		<action bean="F401" />
	</profile>
</profiles>